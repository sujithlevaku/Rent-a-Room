<%= form_with(model: special_price, local: true) do |form| %>
  <% if special_price.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(special_price.errors.count, "error") %> prohibited this special_price from being saved:</h2>

      <ul>
      <% special_price.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :room_id %>
    <%= form.number_field :room_id, id: :special_price_room_id %>
  </div>

  

  <div class="field">
    <%= form.label :start_date %>
    <%= form.date_select :start_date, id: :special_price_start_date %>
  </div>

  <div class="field">
    <%= form.label :end_date %>
    <%= form.date_select :end_date, id: :special_price_end_date %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price, id: :special_price_price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
